! Use Windows-1251 encoding for every .txt and .h files to get valid output !

String Length and Max Length of Words are in file with Hash Function in appropriate folder

The number of every word in "Result.txt", the other statistic in "Analysis.txt", both files are in Output folder


